# Использование API: Конвертация валют

## 📦 Запуск проекта

Для запуска режима разработки выполните:

```bash
yarn dev
```

После запуска сервер будет доступен по адресу:

> http://localhost:9000

### 🔁 Эндпоинт: Конвертация валют

> GET /store/currency/convert

Пример запроса:

> http://localhost:9000/store/currency/convert?amount=5.5&from=USD&to=EUR

📌 Параметры запроса:
| Параметр | Тип | Обязателен | Описание |
| -------- | ------ | ---------- | ------------------------------------------- |
| `amount` | number | да | Положительное число — сумма для конвертации |
| `from` | string | да | Исходная валюта, код **ISO 4217** |
| `to` | string | да | Целевая валюта, код **ISO 4217** |

Так же для успешного запроса надо указать header в запросе x-publishable-api-key, который можно получить на странице администратора:

> http://localhost:9000/app/settings/publishable-api-keys

📝 Пример успешного ответа:

```json
{
  "amount": 5.5,
  "from": "USD",
  "to": "EUR",
  "converted": 4.7619
}
```
